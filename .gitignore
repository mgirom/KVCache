__pycache__/
*.pyc
# generated, hashed, and regenerated by ./auditor/reproduce.sh workload
auditor/workload/haystack.txt
auditor/workload/tasks.json
# runtime state. NOTE: submissions/ is NOT ignored -- it is the published record and
# the reason the store is files in a repo at all. Only a locally-run service's private
# store is ignored, and it lives elsewhere.
site/
_local_submissions/
demo/run/
mscc/accept/kv/
*.npz
*.kvf
*.mscc
